[package]
name = "scenario"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# egui component used to build our simple gui application.
eframe = "0.22"
rfd = "0.15.1"
